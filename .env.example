# 환경변수 템플릿
# 이 파일을 복사해 .env 로 만든 뒤 실제 값을 채워 넣으세요.
#   cp .env.example .env   (Windows PowerShell: Copy-Item .env.example .env)
#
# .env 파일은 .gitignore 에 의해 커밋/푸시되지 않습니다.

APP_ENV=development

# API_KEY=
# DATABASE_URL=
