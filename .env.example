# OpenAI 설정 템플릿
# 실제 값은 .env 에 넣으세요 (.env 는 .gitignore 에 의해 커밋되지 않습니다).

# OpenAI API 키 (https://platform.openai.com → API keys)
OPENAI_API_KEY=sk-...

# 사용할 모델 (생략 시 코드 기본값 gpt-4o-mini)
OPENAI_MODEL=gpt-4o-mini
